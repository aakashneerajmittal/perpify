{
  "name": "@perpify/connect",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Perpify Trader-DNA read-only connect — the server surface that turns a trader's real exchange history into the round-trips the behavioral model scores. Data ingestion + reconstruction core (no secrets).",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
